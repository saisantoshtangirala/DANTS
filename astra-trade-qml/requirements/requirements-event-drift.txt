# Astra-Trade QML - Event-Drift Diagnostic (plain CI runner, no GPU)
#
# TrainingPipeline.event_drift_backtest() involves no ML model training
# at all (it's abnormal-reaction event detection via rolling z-scores +
# a bar-level drift backtest), so this diagnostic runs directly on a
# plain ubuntu-latest GitHub Actions runner instead of a rented RunPod
# GPU pod - see .github/workflows/event-drift-test.yml. Identical to
# requirements-pairs-trading.txt (same reasoning: nothing on this path
# trains a model, but importing TrainingPipeline still transitively
# needs torch/xgboost/statsmodels importable) - kept as its own file so
# the two diagnostics' dependency sets can diverge independently later.

numpy>=1.26
pandas>=2.1
scipy>=1.11
pyyaml>=6.0
python-dotenv>=1.0
python-dateutil>=2.8
pytz>=2023.3

requests>=2.31
yfinance>=0.2.40

scikit-learn>=1.5
xgboost>=2.0
statsmodels>=0.14

--index-url https://download.pytorch.org/whl/cpu
torch>=2.4
--extra-index-url https://pypi.org/simple

SQLAlchemy>=2.0
structlog>=23.1

kiteconnect>=4.2
websocket-client>=1.6
pyotp>=2.9
