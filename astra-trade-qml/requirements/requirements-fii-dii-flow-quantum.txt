# Astra-Trade QML - FII/DII Institutional-Flow Quantum/ML Diagnostic
# (plain CI runner, CPU-only quantum simulation, no RunPod GPU pod)
#
# TrainingPipeline.fii_dii_flow_quantum_backtest() fits
# src/models/quantum/quantum_kernel.py's QuantumKernelClassifier per
# walk-forward fold via qiskit's exact statevector simulation - real
# quantum-circuit simulation work, unlike fii_dii_flow_backtest()'s
# plain rolling-quantile rule, but still CPU-bound (no GPU needed: this
# is the same qiskit stack requirements-training.txt uses for the full
# production ensemble's quantum sub-models, just run here without a
# rented GPU pod since QuantumKernelClassifier's own statevector
# simulation doesn't use one either). Same NSE-archive data sources as
# fii-dii-flow-test.yml - no Kite session needed.

numpy>=1.26
pandas>=2.1
scipy>=1.11
pyyaml>=6.0
python-dotenv>=1.0
python-dateutil>=2.8
pytz>=2023.3

requests>=2.31
yfinance>=0.2.40

scikit-learn>=1.5
xgboost>=2.0
statsmodels>=0.14

--index-url https://download.pytorch.org/whl/cpu
torch>=2.4
--extra-index-url https://pypi.org/simple

SQLAlchemy>=2.0
structlog>=23.1

kiteconnect>=4.2
websocket-client>=1.6
pyotp>=2.9

qiskit>=1.0
qiskit-machine-learning>=0.7
qiskit-algorithms>=0.3
