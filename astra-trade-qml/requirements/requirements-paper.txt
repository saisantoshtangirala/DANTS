# Astra-Trade QML - Paper Trading Environment (Hetzner VPS)
# Lightweight, inference-only dependencies
# Install: pip install -r requirements-paper.txt
#
# Trimmed to what src/ actually imports for paper-trading mode - see
# requirements-training.txt for the full explanation of what was removed
# and why. Versions match what was verified end-to-end against the code.

# Core
numpy>=1.26
pandas>=2.1
scipy>=1.11
pyyaml>=6.0
python-dotenv>=1.0
python-dateutil>=2.8
pytz>=2023.3

# Data
requests>=2.31

# ML Inference Only (NO training frameworks)
scikit-learn>=1.5
xgboost>=2.0
joblib>=1.3

# CPU-only torch for LSTM inference
torch>=2.4
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

# API & Execution (Zerodha Kite)
kiteconnect>=4.2
websocket-client>=1.6
pyotp>=2.9

# Database & Logging
SQLAlchemy>=2.0
structlog>=23.1

# Dashboard
streamlit>=1.26
plotly>=5.16

# Scheduler
APScheduler>=3.10

# Utils
tqdm>=4.66
