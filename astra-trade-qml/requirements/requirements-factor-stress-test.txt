# Astra-Trade QML - Factor Momentum Stress Test (plain CI runner, no GPU)
#
# TrainingPipeline.factor_momentum_stress_test() involves no ML model
# training at all (a paired t-test, a bootstrap resample, a chronological
# split, and a parameter-grid re-run of factor_investing_backtest()'s own
# rolling backtest), so this runs directly on a plain ubuntu-latest
# GitHub Actions runner instead of a rented RunPod GPU pod - see
# .github/workflows/factor-stress-test.yml. Identical to
# requirements-passive-benchmarks.txt (same reasoning: nothing on this
# path trains a model, but importing TrainingPipeline still transitively
# needs torch/xgboost/statsmodels importable) - kept as its own file so
# this diagnostic's dependency set can diverge independently later.

numpy>=1.26
pandas>=2.1
scipy>=1.11
pyyaml>=6.0
python-dotenv>=1.0
python-dateutil>=2.8
pytz>=2023.3

requests>=2.31
yfinance>=0.2.40

scikit-learn>=1.5
xgboost>=2.0
statsmodels>=0.14

--index-url https://download.pytorch.org/whl/cpu
torch>=2.4
--extra-index-url https://pypi.org/simple

SQLAlchemy>=2.0
structlog>=23.1

kiteconnect>=4.2
websocket-client>=1.6
pyotp>=2.9
